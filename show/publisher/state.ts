import { MessageDescriptor } from '@selfage/message/descriptor';

export interface PublisherState {
}

export let PUBLISHER_STATE: MessageDescriptor<PublisherState> = {
  name: 'PublisherState',
  fields: [
  ]
};
