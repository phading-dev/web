import { MessageDescriptor } from '@selfage/message/descriptor';

export interface ConsumerState {
}

export let CONSUMER_STATE: MessageDescriptor<ConsumerState> = {
  name: 'ConsumerState',
  fields: [
  ]
};
