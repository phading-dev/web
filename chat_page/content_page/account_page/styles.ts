import { SCHEME } from "../../common/color_scheme";

export let PAGE_STYLE = `display: flex; flex-flow: column nowrap; width: 100vw; min-height: 100vh;`;
// Needs gap.
export let CARD_STYLE = `margin: auto; display: flex; flex-flow: column nowrap; width: 100%; max-width: 60rem; padding: 5rem; box-sizing: border-box; background-color: ${SCHEME.neutral4}; border-radius: 1rem; gap: 2rem;`;
