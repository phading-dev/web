import { SCHEME } from "../common/color_scheme";

export let PAGE_STYLE = `display: flex; flex-flow: column nowrap; width: 100vw; min-height: 100vh;`;
export let CARD_STYLE = `display: flex; flex-flow: column nowrap; box-sizing: border-box; width: 100%; max-width: 60rem; padding: 3rem; margin: auto; border-radius: 1rem; background-color: ${SCHEME.neutral4};`;
export let TITLE_STYLE = `align-self: center; font-size: 1.6rem; color: ${SCHEME.neutral0};`;
export let SWITCH_TEXT_STYLE = `align-self: flex-end; font-size: 1.4rem; color: ${SCHEME.primary0}; cursor: pointer;`;
