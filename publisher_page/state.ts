import { MessageDescriptor } from '@selfage/message/descriptor';

export interface PublisherPageState {
}

export let PUBLISHER_PAGE_STATE: MessageDescriptor<PublisherPageState> = {
  name: 'PublisherPageState',
  fields: [],
};
