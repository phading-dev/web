import { SCHEME } from "../../../common/color_scheme";
import { FONT_M } from "../../../common/sizes";

export let INPUT_WIDTH = 4; // rem
export let LABEL_STYLE = `font-size: ${FONT_M}rem; color: ${SCHEME.neutral0}; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;`;
